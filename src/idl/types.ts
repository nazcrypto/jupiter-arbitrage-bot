import type { Idl } from '@coral-xyz/anchor';

/**
 * Type definition for the Jupiter routing program (v6).
 * This matches the structure of the IDL located in `idl.json`.
 */
export type JupiterRouteV6 = Idl;
